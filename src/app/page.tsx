
import HomeClientPage from './HomeClientPage';

export default async function HomePage() {
  // All data fetching is now client-side within the specific components
  return (
    <HomeClientPage />
  );
}
