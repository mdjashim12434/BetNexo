
import GlobalLoader from '@/components/shared/GlobalLoader';

// This is the root loading component used by Next.js conventions.
// It wraps our reusable GlobalLoader component.
export default function Loading() {
  return <GlobalLoader />;
}
